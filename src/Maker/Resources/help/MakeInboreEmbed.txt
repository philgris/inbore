The <info>%command.name%</info> command generates InBORe Embed Type.

<info>php %command.full_name% BlogPost</info>

If the argument is missing, the command will ask for the entity class name interactively.